---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: cert-expiry-check
spec:
  schedule: "0 6 * * *"
  jobTemplate:
    spec:
      template:
        spec:
          containers:
            - name: cert-expiry-check
              image: alpine/openssl:latest@sha256:80b347d7b4d58e28aae515cf808ba3ce1c4a2a69a323b2cb2cb2d6d278729684
              command:
                - /bin/sh
                - -c
                - |
                  set -e
                  push() {
                    name="$1"; path="$2"
                    enddate=$(openssl x509 -enddate -noout -in "$path" | cut -d= -f2)
                    epoch=$(date -u -D "%b %d %H:%M:%S %Y %Z" -d "$enddate" +%s)
                    # --post-data via $(...) silently loses its trailing
                    # newline (command substitution always strips it) and
                    # Pushgateway's text-exposition parser requires one on
                    # every line, last line included - write to a file
                    # instead so the newline from printf's redirect survives.
                    printf 'cert_expiry_timestamp_seconds %s\n' "$epoch" > /tmp/metric.txt
                    wget -q -O- --post-file=/tmp/metric.txt \
                      "http://cert-expiry-pushgateway.kube-system.svc.cluster.local:9091/metrics/job/cert-expiry-check/instance/$name"
                    echo "$name: expires $enddate (epoch $epoch)"
                  }
                  push hubble-server-certs /certs/hubble-server-certs/tls.crt
                  push hubble-relay-client-certs /certs/hubble-relay-client-certs/tls.crt
                  push cilium-ca /certs/cilium-ca/ca.crt
              volumeMounts:
                - name: hubble-server-certs
                  mountPath: /certs/hubble-server-certs
                  readOnly: true
                - name: hubble-relay-client-certs
                  mountPath: /certs/hubble-relay-client-certs
                  readOnly: true
                - name: cilium-ca
                  mountPath: /certs/cilium-ca
                  readOnly: true
                - name: tmp
                  mountPath: /tmp
              resources:
                requests:
                  cpu: 10m
                  memory: 16Mi
                limits:
                  cpu: 100m
                  memory: 32Mi
              securityContext:
                allowPrivilegeEscalation: false
                readOnlyRootFilesystem: true
                capabilities:
                  drop: ["ALL"]
          volumes:
            # Only the cert (public) key is mounted from each secret, never
            # the private key half - this job only ever needs to read an
            # expiry date.
            - name: hubble-server-certs
              secret:
                secretName: hubble-server-certs
                items:
                  - key: tls.crt
                    path: tls.crt
            - name: hubble-relay-client-certs
              secret:
                secretName: hubble-relay-client-certs
                items:
                  - key: tls.crt
                    path: tls.crt
            - name: cilium-ca
              secret:
                secretName: cilium-ca
                items:
                  - key: ca.crt
                    path: ca.crt
            - name: tmp
              emptyDir: {}
          restartPolicy: OnFailure
          securityContext:
            runAsNonRoot: true
            runAsUser: 65534
            runAsGroup: 65534
            seccompProfile:
              type: RuntimeDefault
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 1
