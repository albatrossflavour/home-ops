# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/external-secrets.io/externalsecret_v1.json
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: unifi2netbox
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword-connect
  target:
    name: unifi2netbox-secret
    template:
      engineVersion: v2
      data:
        UNIFI_USER: "{{ .username }}"
        UNIFI_PASSWORD: "{{ .password }}"
        UNIFI_MFA_SECRET: "{{ .TOTP_57EF6F72598D481DB84F02A12ABC2409 }}"
        NETBOX_API_TOKEN: "{{ .NETBOX_API }}"
  dataFrom:
    - extract:
        key: ubiquti
    - extract:
        key: netbox
